services:
  - type: web
    name: book-api
    runtime: node
    buildCommand: npm install
    startCommand: npm run start:api -- --port $PORT
    envVars:
      - key: NODE_VERSION
        value: 20
